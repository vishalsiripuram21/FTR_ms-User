server:
  port: 3333
spring:
  application:
    name: FTR_ms-User
  cloud:
    consul:
     discovery:
      hostname: localhost
     config:
      enabled: true
      prefix: config
      default-context: application
      profile-separator: '::'
      format: YAML
